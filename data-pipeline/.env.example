# GCP (required for Vertex AI billing)
GCP_PROJECT_ID=your-gcp-project-id
GCP_REGION=us-central1
GCS_RAW_BUCKET=customsguard-raw
GCS_PROCESSED_BUCKET=customsguard-processed

# Database (Cloud SQL or local Docker)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ai_saas_db
DB_USER=postgres
DB_PASSWORD=postgres

# Google AI Studio (embedding free tier only)
GEMINI_API_KEY=your-gemini-api-key
